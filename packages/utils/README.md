# @template/utils

Pure utility functions used across packages and apps.

## Public API

- `cn(...inputs)` — Merge Tailwind class names

## Adding utilities

- Pure functions only (no side effects)
- No external dependencies beyond what's in package.json
- Document with JSDoc
